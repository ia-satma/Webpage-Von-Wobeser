node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Local environment (secrets — never commit)
.env
.env.local
.env.*.local
# Archivos con API keys (no commitear)
CLAUDEAPI.env.rtf
*.env.rtf

# Uploads de runtime (contenido de usuario, no se commitea)
uploads/*
!uploads/.gitkeep
private_uploads/

# Paquetes portables de entrega (base cifrada y exportación de App Storage)
# Se transfieren por un canal seguro; nunca deben publicarse en GitHub.
.handoff/
# Basura macOS
__MACOSX/
._*
.DS_Store
